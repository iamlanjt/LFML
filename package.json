{
  "name": "lolcode_joiner",
  "version": "1.0.0",
  "description": "A module to utilize faux lolcode modules, since its not supported in the LCI interpreter.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
	"start": "node src/index.js --target=./test"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^11.1.0"
  }
}
